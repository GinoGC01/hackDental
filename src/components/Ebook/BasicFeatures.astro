---
// src/components/Features.astro
import Button from './Button.astro';

const beforeSlides = [
  { image: "../iamges/imagen x derecha.svg", alt: "Posteos Genéricos" },
  { image: "../iamges/imagen x derecha.svg", alt: "Sillones Vacíos" },
  { image: "../iamges/imagen x derecha.svg", alt: "Estrés Constante" }
];

const afterSlides = [
  { image: "../iamges/imagen-1.svg", alt: "Anuncios Virales" },
  { image: "../iamges/imagen-1.svg", alt: "Agenda Colapsada" },
  { image: "../iamges/imagen-1.svg", alt: "Libertad Real" }
];
---

<section class="py-12 md:py-24 bg-white relative overflow-hidden">
  <div class="absolute inset-0 bg-[url('https://www.transparenttextures.com/patterns/graphy.png')] opacity-5 pointer-events-none"></div>

  <div class="max-w-7xl mx-auto px-4 relative z-10">
    
    <div class="text-center mb-10 md:mb-16 max-w-4xl mx-auto">
      <div class="inline-block bg-brand-red/10 text-brand-red px-3 py-1 rounded-full text-[10px] md:text-xs font-bold uppercase tracking-widest mb-3 border border-brand-red/20">
        📖 Transformación Real
      </div>
      <h2 class="text-3xl md:text-5xl font-black font-heading text-gray-900 uppercase leading-[0.9] mb-3">
        He transformado completamente <br/>
        <span class="text-transparent bg-clip-text bg-gradient-to-r from-red-600 to-red-400 italic pr-2">mi clínica dental.</span>
      </h2>
    </div>

    <div class="bg-gray-100 rounded-2xl md:rounded-[2rem] p-4 md:p-12 mb-16  shadow-inner relative overflow-hidden">
       
       <div class="grid md:grid-cols-12 gap-6 items-center">
          
          <div class="md:col-span-5 relative group overflow-hidden rounded-xl bg-white shadow-md">
              <div class="absolute top-2 left-2 z-20 bg-gray-200/90 backdrop-blur-sm text-gray-600 text-[10px] font-bold px-2 py-1 rounded-full border border-gray-300">
                  ANTES 😭
              </div>

              <div class="h-[320px] md:h-[380px] w-full relative overflow-hidden">
                 <div class="carousel-track-left flex w-[200%] h-full">
                    <div class="flex w-1/2 h-full">
                        {beforeSlides.map((slide) => (
                          <div class="w-full flex-shrink-0">
                            <img src={slide.image} alt={slide.alt} class="w-full h-full object-contain"/>
                          </div>
                        ))}
                    </div>
                    <div class="flex w-1/2 h-full">
                        {beforeSlides.map((slide) => (
                          <div class="w-full flex-shrink-0">
                            <img src={slide.image} alt={slide.alt} class="w-full h-full object-contain"/>
                          </div>
                        ))}
                    </div>
                 </div>
              </div>
          </div>

          <div class="md:col-span-2 text-center py-4 md:py-0">
              <h3 class="text-2xl md:text-4xl font-black font-heading text-gray-400 uppercase tracking-widest">
                HACIA...
              </h3>
          </div>

          <div class="md:col-span-5 relative group overflow-hidden rounded-xl bg-white shadow-2xl scale-105 z-10">
              <div class="absolute top-2 right-2 z-20 bg-brand-red text-white text-[10px] font-bold px-2 py-1 rounded-full shadow-lg">
                  AHORA 🚀
              </div>

              <div class="h-[320px] md:h-[380px] w-full relative overflow-hidden">
                 <div class="carousel-track-right flex w-[200%] h-full">
                    <div class="flex w-1/2 h-full">
                        {afterSlides.map((slide) => (
                          <div class="w-full flex-shrink-0">
                            <img src={slide.image} alt={slide.alt} class="w-full h-full object-contain"/>
                          </div>
                        ))}
                    </div>
                    <div class="flex w-1/2 h-full">
                        {afterSlides.map((slide) => (
                          <div class="w-full flex-shrink-0">
                            <img src={slide.image} alt={slide.alt} class="w-full h-full object-contain"/>
                          </div>
                        ))}
                    </div>
                 </div>
              </div>
          </div>

       </div>
    </div>

    <div class="mt-12 text-center">
        <Button className="px-12 py-4 text-xl">QUIERO ESTA TRANSFORMACIÓN</Button>
    </div>
  </div>
</section>

<style>
  /* Estrategia: 
     El track mide 200% (dos bloques de imágenes).
     Desplazamos -50% (un bloque entero). 
     Al llegar al final del primer bloque, el segundo es idéntico, 
     el navegador reinicia a 0% y el ojo no nota el salto.
  */

  @keyframes scroll-left {
    0% { transform: translateX(0); }
    100% { transform: translateX(-50%); }
  }

  @keyframes scroll-right {
    0% { transform: translateX(-50%); }
    100% { transform: translateX(0); }
  }

  .carousel-track-left {
    /* 10s para 3 imágenes es una velocidad legible. Ajusta según necesites */
    animation: scroll-left 2s linear infinite;
  }

  .carousel-track-right {
    animation: scroll-right 2s linear infinite;
  }

  /* Pausa al hacer hover para mejorar UX */
  .group:hover .carousel-track-left,
  .group:hover .carousel-track-right {
    animation-play-state: paused;
  }
</style>